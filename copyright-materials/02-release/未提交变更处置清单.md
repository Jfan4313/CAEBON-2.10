# V2.14 未提交变更处置清单

基线审计时存在 48 项左右修改或新增内容。本轮未执行 `git reset --hard`、`git checkout --` 或覆盖式清理。

## 纳入 V2.14 候选源码

| 类别 | 文件/目录 | 处置结论 |
|---|---|---|
| 应用入口与身份 | `App.tsx`、`types.ts`、`components/Sidebar.tsx`、`shared/config/productIdentity.ts`、`shared/components/AboutSoftware.tsx` | 纳入；统一V2.14身份并隐藏未完成功能 |
| 项目与账单 | `components/ProjectEntry.tsx`、`components/ProjectManager.tsx`、`components/project-entry/*` | 纳入；属于正式数据录入与项目管理 |
| 配置与状态 | `context/ConfigContext.tsx`、`context/ProjectContext.tsx`、`context/initialData.ts` | 纳入；属于正式状态与默认数据 |
| 光伏 | `modules/solar/` | 纳入稳定业务代码；预研/未授权视觉资产除外 |
| 储能 | `modules/storage/`、`shared/utils/solarStorageOptimization.ts` | 纳入稳定测算、推荐和报告代码 |
| 综合报告 | `components/DetailedReport.tsx`、`components/OnePageReport.tsx`、`components/ReportCenter.tsx`、`shared/reporting/`、`utils/reportExport.ts`、`utils/excelExport.ts` | 纳入稳定的PDF/Excel相关代码 |
| 存储 | `services/projectStorage.ts`、`services/storage-adapter.ts`、`types/projectStorage.ts` | 纳入本地存储与迁移代码 |
| 其他稳定板块 | `modules/ev/`、`modules/energy-sales/`、现有HVAC/照明/热水稳定组件 | 仅纳入经过界面回归确认的部分 |

## 延期或不纳入申报范围

- `components/RetrofitAI.tsx`、`components/RetrofitVPP.tsx`、`components/RetrofitCarbon.tsx` 及对应预研模块：源码可保留在研发仓库，V2.14入口隐藏，源程序鉴别材料排除。
- 实时微电网、IoT、数字孪生和云端多人协同：不作为已完成功能；可选Supabase代码不进入自主源程序材料。
- `public/solar-construction/`：图片授权未确认前不进入申报包。

## 明确排除

- `.env`、`.env.development`、`node_modules/`、`dist/`、`dist-electron/`、`outputs/`、`tmp/`。
- 历史个人路径辅助脚本、重复 `electron/main 2.js`、系统文件、日志、预览图、测试输出。
- 第三方框架、SDK、字体、图标、图片和自动生成代码。

## 冻结状态

代码构建和标准算例已通过，但最终提交/标签尚未创建。原因：申请人、参与者关系、图片授权和首次发表事实尚待用户确认；在这些事实确认前创建“最终申报”标签会形成错误的完成表述。

